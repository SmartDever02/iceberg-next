'use client';

/*
This is a for having toast container component as client component
*/

import { ToastContainer } from 'react-toastify';

export default function ClientToastContainer() {
  return <ToastContainer />;
}
